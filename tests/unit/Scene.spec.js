import {mount} from '@vue/test-utils';
import Scene from '@/components/Scene';

describe('Scene.js', () => {
    test('should add default scene when created', () => {
        const vm = mount(Scene);
    });
});